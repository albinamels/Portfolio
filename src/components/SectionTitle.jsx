const SectionTitle = ({ text }) => {
  return (
    <div className="border-b-4 pb-5">
      <h2 className="text-3xl font-medium text-slate-700 capitalize">{text}</h2>
    </div>
  );
};

export default SectionTitle;
